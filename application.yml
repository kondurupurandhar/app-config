---
spring:
  profile: cloud
  eureka:
    instance:
      hostname: ${vcap.application.uris[0]}
      nonSecurePort: 80
      metadataMap:
        instanceId: ${vcap.application.instance_id}
spring:
  profile: dev
  eureka:
    client:    
      serviceUrl:
        defaultZone: http://service-registry-worrisome-counsellor.cfapps.io/eureka/
